"""Django admin registrations for the feature app.

Currently empty — `feature` defines no persistent models (CVD is
computed on the fly from `data.Candle.delta`). The module stays so a
future `feature` model gets a conventional registration site.
"""
